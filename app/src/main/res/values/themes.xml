<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CommentDiary" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/coda_background</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/coda_common_text</item>
        <item name="android:textColorHint">@color/coda_common_text_hint</item>
        <item name="android:colorBackground">@color/coda_background</item>
        <item name="android:windowLightStatusBar">true</item>
<!--        android:windowBackground : 이 설정은 window의 배경을 설정하는 것이다. color 또는 drawable 설정이 가능하다. -->
<!--        <item name="android:windowBackground">@color/coda_background</item>-->
<!--        <item name="android:windowSplashScreenBackground">@color/coda_common_text</item>-->
    </style>
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
<!--        <item name="android:colorBackground">@color/onboarding_red</item>-->
    </style>
</resources>